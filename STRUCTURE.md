# Structure

```
framework/      core theory — primitives, identity, topology
experiments/    validation series — arc document + G through R
code/           executable experiments — self-contained Python scripts
app/            interactive explorer — Vite React, runs in browser
```

Start with [`experiments/experiment_arc.md`](experiments/experiment_arc.md).
